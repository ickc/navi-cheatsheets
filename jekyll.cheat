% jekyll

# jekyll:serve — bundle exec jekyll serve with drafts, profiling, and incremental rebuilds
<ENV> bundle exec jekyll serve --profile --drafts --incremental

; prepend JEKYLL_ENV=production to render the site as it would deploy
$ ENV: echo -e " \nJEKYLL_ENV=production"
